# AI-for-medical-Imaging

os: environment variable (API key), file delete, file path checks

time: retry backoff on rate limits

uuid: unique temp filenames so collisions don’t happen

datetime: timestamp for history + downloads

BytesIO + reportlab: make PDF in memory, then download

streamlit: UI (tabs, sidebar, buttons)

Pillow: image open/resize

agno + Gemini: AI agent that calls Gemini API

DuckDuckGoTools: optional web search (references)
